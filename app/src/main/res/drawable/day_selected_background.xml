<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- 선택된 날 채움색(기존 유지) -->
    <solid android:color="#1DFFFFFF" />

    <!-- 두께: dimen 참조 -->
    <stroke
        android:width="1dp"
        android:color="#E4212121" />

    <!-- 지름: dimen 참조 -->
    <size
        android:width="@dimen/day_circle_diameter"
        android:height="@dimen/day_circle_diameter" />
</shape>
